# Multi-node kind cluster for local CI/CD and canary demo.
# 1 control-plane + 1 worker.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: llm-k8s
nodes:
  - role: control-plane
  - role: worker
